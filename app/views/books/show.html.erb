<section class="show-book">
  <h2 class="book-box">
    <p>Title:</p><%= @book.title %>
  </h2>

  <h2 class="book-box">
    <p>Body:</p><%= @book.body %>
  </h2>

  <%= link_to "Edit", edit_book_path(book.id) %> | <%= link_to "Back", book_path %>
</section>